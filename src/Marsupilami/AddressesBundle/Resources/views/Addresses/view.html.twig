{% extends "MarsupilamiAddressesBundle::layout.html.twig" %}

{% block title %}
  Lecture d'une adresse - {{ parent() }}
{% endblock %}

{% block marsupilamiaddresses_body %}

  <h2>{{ addresses.title }}</h2>
  <i>Le {{ addresses.date|date('d/m/Y') }}</i>

  <div class="well">
    {{ addresses.content }}
  </div>

  <p>
    <a href="{{ path('marsupilami_addresses_home') }}" class="btn btn-default">
      <i class="glyphicon glyphicon-chevron-left"></i>
      Retour à la liste
    </a>
    <a href="{{ path('marsupilami_addresses_edit', {'id': addresses.id}) }}" class="btn btn-default">
      <i class="glyphicon glyphicon-edit"></i>
      Modifier l'adresse
    </a>
    <a href="{{ path('marsupilami_addresses_delete', {'id': addresses.id}) }}" class="btn btn-danger">
      <i class="glyphicon glyphicon-trash"></i>
      Supprimer l'adresse
    </a>
  </p>

{% endblock %}